import 'package:flutter/material.dart';

class BlogDetailImageScreen extends StatelessWidget {
  const BlogDetailImageScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}

import 'package:flutter/material.dart';

class BlogTabImageList extends StatelessWidget {
  const BlogTabImageList({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('BlogTabImageListWidget'),
    );
  }
}
